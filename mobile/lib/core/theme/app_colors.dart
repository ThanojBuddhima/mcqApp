import 'package:flutter/material.dart';

class AppColors {
  // Core palette — black & white
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);
  static const background = Color(0xFFFFFFFF);
  static const surface = Color(0xFFF5F5F5);
  static const surfaceHover = Color(0xFFEBEBEB);
  static const border = Color(0xFFE0E0E0);
  static const borderStrong = Color(0xFF1A1A1A);

  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF757575);
  static const textTertiary = Color(0xFFB0B0B0);

  // Accent — only for primary CTAs (Submit, Start, Upload)
  static const accent = Color(0xFFFF5722);

  // Semantic — only for status feedback
  static const success = Color(0xFF22C55E);
  static const error = Color(0xFFEF4444);
}
